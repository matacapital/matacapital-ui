import { definition as globalDefinition } from "../../utils/styles-map/definition.ts";

export const definition: Record<string, any> = {
  ...globalDefinition,
  size: {
    s: "ml-xs",
    m: "ml-l",
  },
};
